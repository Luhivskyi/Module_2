function createArrayOfNumbers(min, max) {
  const numbers = [];
  // Пиши код ниже этой строки
  
   for(;min <= max; min++) {
     
  numbers.push(min);
     
   }
  // Пиши код выше этой строки
  return numbers;
}